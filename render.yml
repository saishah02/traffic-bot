services:
  - type: web
    name: traffic-bot
    env: python
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
      python manage.py migrate
    startCommand: gunicorn gps_project.wsgi
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: gps_project.settings